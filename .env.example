# Copy to .env and fill in. .env is gitignored; never commit a key.
# Use a throwaway key funded only with testnet gas on the chain you deploy to.
PRIVATE_KEY=
# Optional: a private Base Sepolia RPC if the public one rate-limits you.
BASE_SEPOLIA_RPC=
